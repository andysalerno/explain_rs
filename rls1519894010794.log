thread 'request-worker-1' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
note: Run with `RUST_BACKTRACE=1` for a backtrace.
thread 'request-worker-2' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-4' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-2' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-7' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-0' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-0' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-4' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-5' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-7' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-3' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-7' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-0' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-1' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-5' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-2' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-2' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
thread 'request-worker-7' panicked at 'byte index 18446744073709551615 is out of bounds of `use std::env;
use std::fs::File;
use std::io::Read;

fn main() {
    let args: Vec<String> = env::args().collect();

    if args.len() < 2 {
        print_usage();
        return;
    }

    let command_name = &args[1];

    let man_text = read_man_page(co`[...]', libcore/str/mod.rs:2221:9
